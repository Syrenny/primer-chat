[project]
name = "generation-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.12.0",
    "asyncio>=3.4.3",
    "asyncio-throttle>=1.0.2",
    "backoff>=2.2.1",
    "jinja2>=3.1.6",
    "loguru>=0.7.3",
    "pydantic-settings>=2.10.1",
    "pyyaml>=6.0.2",
    "shared-adapters",
    "shared-kafka",
]

[tool.uv.sources]
shared-adapters = { path = "../../shared-adapters", editable = true }
shared-kafka = { path = "../../shared-kafka", editable = true }
